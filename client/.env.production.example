# Copia in client/.env.production e sostituisci <username> con il tuo utente PartyKit.
# Lo trovi dopo "npm run deploy:party" (URL tipo https://v-editor.<username>.partykit.dev).
VITE_PARTYKIT_HOST=v-editor.<username>.partykit.dev
